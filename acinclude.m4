dnl acinclude.m4 stub — XMMS macros moved to m4/xmms-macros.m4
# The original acinclude.m4 included a copy of libtool macros (old serial)
# which caused libtool LT_INIT version mismatches during builds.
# We moved the XMMS-specific macros to m4/xmms-macros.m4 and rely on
# the system libtool macros (installed into m4/ by libtoolize) instead.
# The original file is backed up at acinclude.m4.bak
